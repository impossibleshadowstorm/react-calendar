import { Box } from "@mui/material";
import React from "react";

const EventModal = () => {
  return (
    <Box
      sx={{
        height: "100vh",
        width: "100%",
        left: 0,
        top: 0,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <form style={{}}></form>
    </Box>
  );
};

export default EventModal;
